//use reduce to calculate the values of each dollars
// let account = [{dolars:2}, {dolars:5}, {dolars:10},{dolars:100}];
// array = [1, 2]


// function reduce(arr) {
//         let sum = 0;
//         arr.forEach((item) => {
//           sum = sum + item;
//         })
//         return sum;
//     }
//     console.log(reduce([1, 2]));


// let account = [{dolars:2}, {dolars:5}, {dolars:10},{dolars:100}];
// let b = account.reduce( (a, b) => {
//     return {dolars: a.dolars + b.dolars}; // returns object with property x
//   })
// console.log(b.dolars)




  

//idea project - an easy applicable website for jobs * 




//   function myFunc(theObject) {
//       // 1. that is a function that has the name myFunc with a parameter
//       //calling = myFunc()
//   }
//   var myFunction = function() {
//     // 2. that is a function holding in the variable called myFunction, but the function
//     // itself has no name - thats an annnymus function
//     //callling myFunction()
// }

// var myFunction = function namedFunction(){
//     // 3. the same as 1, but not holding in a variable
//     // that is a function holding in the variable called myFunction, and the function
//     //has a name called namedFunction and a parameter
//     //myFunction(), or namedFunction()
// }


// (function() {
//     // 4. there is an annonymus function wraped in parantheses, with the statement {}, and calling it
//     //right away
//     //calling it already, or call it doing () at the end of the function
// })()

// (param) => {
//     // 5. there is an annonymus ARROW function (WITHOUT A NAME) with a parameter called param
//     // not calling, unless at the end () right away
//     //super useful to pass inside of another function as a parameter
//     // calling it right away with (), or save it in a variable and then call it like variable(),
//     //otherwise it just consoles...
// }

// param => statement
// // 6. the same as number 5, but without parantheses in the parameter, an annnymus function
// // without the curly braket around the statement
// //arrow annonymus function 


// new Function (param, functionBody)
// //7. thats creating an annonnymus function (new Function), parameter inside parantheses and the statement
// // let daria = new Function ('num' parameter inside as a string, 'return (num+1)' or another statement inside)
// //calling it saving in variable 

// __________________________________________________]

// for(i = 0;i < list.length; i++){
//     statement 
// }
// for (index in list){
// //bad practice 
// }

// for (item for list){

// }

// while 

// while do

// __________________________________________________

// if (condition){
//     statement
// }

// index1 > index ? console.log('maira'): console.log('daria')

// index > index2 && index1 > index3 ? do this : do that

// id1 > id2 ? (id3 > id4 ? console.log(1) : console.log(2)) : console.log("not")









